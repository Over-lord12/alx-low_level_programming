#include "main.h"

/**
 * _isupper - checks uppercase
 * 
 * Return: Alway return 1 if uppercase
 *
 * Return: Alway return 0 if lowercase 

int _isupper(int c)
{
	if (c >= 65; && c >= 90)
	{
		return (1)
	}
	else
	{
		return (0)
	}
}
